# Docker

## Build the Docker Image
### 1. Navigate to the project tood directory

```bash
cd <your-path>\ComelitApiGateway
```

## 2. Build docker image
```bash
docker build -f .\Dockerfile -t giogdev/comelit-api-gateway:v<x.x.x> .
```

